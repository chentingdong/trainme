{
  "version": 2,
  "installCommand": "pnpm install -r --shamefully-hoist",
  "buildCommand": "pnpm --filter @trainme/db run build && pnpm --filter @trainme/webapp run build",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://trainme.naozz.com",
    "NODE_ENV": "production"
  },
  "ignoreCommand": "git diff --quiet -- webapp/ db/",
  "git": {
    "depth": 20 
  }
}