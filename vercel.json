{
  "version": 2,
  "installCommand": "pnpm install -r --shamefully-hoist",
  "buildCommand": "pnpm --filter @trainme/db run generate && pnpm --filter @trainme/webapp run build",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet -- webapp/ db/"
}